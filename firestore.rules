
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // ATENÇÃO: As regras foram abertas para permitir o desenvolvimento.
    // A autenticação será reimplementada.

    match /{path=**} {
      allow read, write: if true;
    }
  }
}
