
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // As regras agora exigem que o usuário esteja autenticado.
    match /{path=**} {
      allow read, write: if request.auth != null;
    }
  }
}
